{
  "description": "Auto-log hook that records conversations and code changes after each Claude Code reply",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/scripts/auto-log.js\""
          }
        ]
      }
    ]
  }
}
